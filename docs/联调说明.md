# 联调与验收

> 更新于 2026-09-21。本文提供可执行检查步骤，不沿用旧阶段报告的测试数字或通过结论。自动化替身、浏览器检查、真实设备和真实供应商验收分别记录。

## 1. 启动与配置检查

### 像素边界修复与现有接口对照（2026-09-21）

按用户选择，本轮继续使用已有识图接口，没有接入新图像编辑服务。已启用三项确定性修复：

- 双点/轮廓接触先走原 256 网格；圆头笔、铅笔的边界冲突再用原始像素与实际笔尖覆盖范围精查。接触带、必需接点、原画重叠比例、复核后位置均未放宽。矩形、颗粒和星形笔尖暂保留原有保守判断。
- 有原始像素时，接点必须附近存在同时被新增笔迹覆盖的原墨迹，拦截“双方都靠近接点，但中间仍有白缝”。前端在准备、投影和提交阶段读取当前画布，版本变更仍阻止提交；缺失/不一致像素不能消除网格冲突，多方案也不能借原图像素绕过相互碰撞。
- 规划场景与轮廓测量共用 `niloObservedRegions.js`。剔除越界、过小或无名部位后，剩余部位的 ID 保持一致，避免合法接点被丢弃或错配。

旧冻结控制集：19 个预期可落笔方案全部通过（此前 17/19），包含此前误拒的圆脸、歪头双端帽子；12 个非法引用仍全部拒绝。覆盖原画、虚假接点、白缝、原线重描等额外反例均通过拒绝测试。刻意语义错误的控制仍可通过纯几何检查，因此这不是 AI 成功率。两顶帽子是作者控制方案，不能冒充模型自主生成的成功案例。

另建 20 张新冻结合成图，在现有模型下比较原观察方式与“真实笔迹区域编号选择”，每图每方案一次、顺序交错；未把作者答案送给模型。新清单见 [fresh-scenes-v1-manifest.json](../server/evals/nilo/fresh-scenes-v1-manifest.json)，结果见 [2026-09-21-contact-fix.json](../server/evals/nilo/2026-09-21-contact-fix.json)。

| 观察方案 | 可落笔 / 全部 | P50 / P95 | 调用数 | 供应商报告总 tokens |
| --- | ---: | --- | ---: | ---: |
| 原方案 | 10 / 20 | 4.6 / 6.9 秒 | 57 | 141,583 |
| 区域编号实验 | 7 / 20 | 4.5 / 6.4 秒 | 48 | 111,732 |

编号实验没有未知 ID 或自行返回坐标的问题，但仍把语义名称对应到错误区域，且更多主体低于原置信门槛。编码助手查看全部 17 张成功 after 图：有可理解的口袋、菌盖斑点、鳃线和车窗，也有花外“叶脉”、不连接任何框的“短桥”、脸部“胸口纽扣”等错误。独立人工语义准确率保持未评估；不能用模型自审或几何通过率代替。编号实验不启用，不调整置信门槛来抬高通过数。

该对照包含像素边界与接点精查；统一区域编号的最后修复在实验之后完成并单独回归，因此本表用于路线选择，不是最终版本的准确率或相对上一轮的提升。新冻结图首次执行后已成为回归集。程序控制集与真实模型结果分别保存，不混合计数。

```bash
# 离线几何回归，不调用模型
node server/scripts/check-nilo-ablation.mjs --modes=gold-plan
# 准备 20 张图与未运行记录；不加 --live 就不会调用供应商
node server/scripts/check-nilo-fresh-scenes.mjs
# 复跑只计为回归，--live 会产生供应商用量
node server/scripts/check-nilo-fresh-scenes.mjs --live --retest
```

最终自动化：后端全量 563 项、前端全量 384 项通过，前端生产构建通过，lint 无错误、3 条既有警告。已确认本地后端在最后生产改动后自动重载，健康接口正常。本轮未进行真实浏览器、手机或儿童体验验收。

局部图像新增图层的离线验证模块已准备，但按用户要求未配置新服务、未生成真实供应商图片、未接入应用。它只验证给定 PNG 的范围和原墨保留，不能证明语义或画风正确。

### 分层对照与轮廓贴合（2026-09-21）

已完成模型分层对照与通用接触改造，同时接入语义/格式纠正分流。默认模型、画布布局和调用上限未更换。新增的 32 格点序列编译器、关系添画案例库已完成小样本实验，尚未接入默认模型链路；当前证据指向观察/区域定位为优先瓶颈，不能凭新绘图格式或案例数量宣称改善。

新的 12 张合成图在首次 live 前冻结，覆盖人物、动物、植物、家具、交通、想象与抽象。fixture 和 SHA-256 见 [ablation-v1-manifest.json](../server/evals/nilo/ablation-v1-manifest.json)。模型对照共 48 个回合，固定图片、协议、14 秒预算及最多两次规划/两次复核；原始图、失败候选、模型输出与延迟均保留在本地忽略目录。永久聚合见 [2026-09-21-ablation.json](../server/evals/nilo/2026-09-21-ablation.json)。

| 模式 | 模型 | 可落笔 / 全部样例 | P50 / P95 | 供应商报告总 tokens |
| --- | --- | --- | --- | ---: |
| 完整观察→规划→复核 | 当前 vision-exp | 5 / 12 | 4.2 / 6.9 秒 | 79,627 |
| 提供作者确认的主体/区域→规划→复核 | 当前 vision-exp | 9 / 12 | 2.9 / 5.6 秒 | 64,463 |
| 完整观察→规划→复核 | 同供应商 v4-pro 候选 | 1 / 12 | 10.7 / 13.6 秒 | 74,470 |
| 提供作者确认的主体/区域→规划→复核 | 同供应商 v4-pro 候选 | 0 / 12 | 7.7 / 11.8 秒 | 62,328 |

完整链路模型 ID 为 `deepseek-v4-flash-vision-exp`，候选为供应商模型列表返回的 `deepseek-v4-pro`。接口接受图片不等于视觉能力达标；候选多次描述原图没有的颜色或物体，未通过本轮质量筛选，因此不切换生产。以上为小样本单轮结果，不是模型排名；gold-scene 少一次观察，延迟/tokens 不可直接当同等任务的节省。未查询价格，不估算金额。

编码助手查看当前模型完整链路的 5 张 after 图并核对原始意图：盆栽右叶脉初步合理；头顶蝴蝶结能看懂但连接形式生硬；“小痣”实际是偏大的空心圆；椅子仍被当成画架，新增矩形虽位于椅背却不符合模型声称的含义；小旗悬空，没有接到桅杆。故 **5/12 不是有效共创率，80% 目标尚未达成**，也不能与上一轮不同的 11 张样例直接比较提升。

7 个失败中，3 个存在“身份置信度低于门槛，但部分子区域自报置信度较高”：长身体小狗、分叉树、相连的环。子区域自评分不能直接当作经过验证的几何能力；本轮没有降低识别门槛来换落笔率。后续需要把身份假设、独立几何描述和真实笔迹支撑分开验证。

离线另有 32 个作者控制：19 个预期可放置的方案中 17 个通过，12 个非法引用全部拒绝，1 个刻意语义错误仍可通过几何，证明必须单独评估语义。3 个沿轮廓帽沿全部通过；圆脸、歪头的双端帽子各有保守误拒。逐像素检查显示误拒来自方形膨胀足迹与椭圆接触许可的粗栅格差异；提高 map 至 512/1024 未稳定改善，所有测试组 9 个额外覆盖/假接触负例仍被拒，因而未升级默认位图或放宽保护。

```bash
# 离线：人工计划→真实编译与放置，不调用供应商
node server/scripts/check-nilo-ablation.mjs --modes=gold-plan --out=server/.tmp/nilo-ablation
# 重跑已使用的冻结样例必须计为回归；--live 会产生用量
node server/scripts/check-nilo-ablation.mjs --live --retest --modes=full,gold-scene --models=current,deepseek-v4-pro --out=server/.tmp/nilo-ablation
```

接触约束、实际轮廓测量、前端保留字段及语义纠正已自动化验证。语义纠正只能选已有场景中的替代目标，不能捏造观察结果；所有替代仍重走编译、碰撞和像素复核。详细 wire contract 见 [Nilo API](../server/NILO_API.md)。

本地后端已重载，健康接口正常；通过实际 HTTP 用合成人脸明确请求加帽子，返回 v2，约 5.5 秒后因候选未通过预检进入恢复交互。这次失败单独保留，不算冻结批次或成功案例。它也说明“程序能正确放置作者设计的帽子”不等于“模型每次都能生成合法帽子”。

另外选取冻结图中的圆脸、歪头、猫、树、椅子、相连环，提供作者确认的场景，各运行一次网格描点和关系案例实验；复用首次 gold-scene 的同 6 条记录作为基线，不额外重跑挑选结果。

| 六图回归子集 | 可落笔 | P50 / P95 | 调用数 | 供应商报告总 tokens |
| --- | ---: | --- | ---: | ---: |
| 复用的原方案 | 4 / 6 | 2.9 / 5.5 秒 | 15 | 33,665 |
| 32 格网格描点 | 2 / 6 | 3.9 / 6.2 秒 | 17 | 38,986 |
| 检索添画关系案例 | 4 / 6 | 2.8 / 4.3 秒 | 12 | 31,746 |

这 12 个新增回合是小样本、非随机、非同步回归，不是新的留出集，不能据此证明速度或质量提升。编码助手查看全部 6 张通过后的图片：网格方案的卷毛有一段悬空；猫胡须仅在一侧。案例方案的眉毛偏到两眼之间、椅背“横木”只是未连接的短弧；猫嘴附近弧线与环内圆点较易理解，但仍不足以证明有效共创改善。语义通过率保持未评估。两项实验均不切换默认，原始失败和聚合结果全部保留。

```bash
# 独立实验；--live 会产生供应商用量，重跑只能视为回归
node server/scripts/check-nilo-drawing-pilots.mjs --pilot=waypoints --live
node server/scripts/check-nilo-drawing-pilots.mjs --pilot=relations --live
# 仅渲染原创关系案例，不调用供应商
node server/scripts/render-nilo-relation-examples.mjs
```

自动化验证：后端全量 431 项通过，随后两个独立实验模块新增的 54 项通过，合计覆盖当前 485 项；前端全量 379 项通过，生产构建通过，lint 无错误、3 条既有警告。实验 CLI 的离线运行和注入式冒烟通过；未进行本轮浏览器截图、真实儿童或移动设备验收。自动化通过只证明程序约束，不代表模型共创质量达标。

### 五项修复验收（2026-09-21）

本轮五项工程改造均已接入：意图/几何分离、统一图元协议、保留意图的有限纠正、前后端共享碰撞检查、跨画作评测。当前点击使用 v2；普通语音与旧客户端保留 v1。后文 9 月 20 日记录是历史结果，不能替代本轮验收。协议见 [Nilo API](../server/NILO_API.md)，聚合结果见 [2026-09-21.json](../server/evals/nilo/2026-09-21.json)。

自动化：后端 383 项、前端 376 项通过，生产构建通过；lint 无错误、3 条既有警告。测试覆盖跨主体统一契约、缺失字段、有限格式恢复、候选切换、意图锁定、同一碰撞位图、固定连接点/方向、超时与取消。未进行本轮真实浏览器截图或儿童体验验收。

真实模型：使用当前配置的 `deepseek-v4-flash-vision-exp`，仅发送程序生成的图。每个样例一轮，失败也计入分母；没有为了拿成功率而重复直到成功。

| 组别 | 样本 | 至少一项可编译 | 至少一项几何预检通过 | 实际画布可落笔 | P50 / P95 |
| --- | ---: | ---: | ---: | ---: | --- |
| 最初未调参的留出集 | 6 | 4 | 3 | 2 | 约 4.8 / 5.4 秒 |
| 最终开发回归（树、人、猫、机器人、吉他） | 5 | 3 | 2 | 2 | 4.1 / 6.4 秒 |
| 最终扩展回归（象、火箭、椅子、盆栽、想象生物、抽象画） | 6 | 4 | 2 | 2 | 3.5 / 5.4 秒 |

留出集首次运行后用于定位通用编译问题，之后必须标为回归集，不能再声称是未见样本；后续质量验收需要新冻结样本。保留首次失败记录。延迟分位数采用 nearest-rank，失败请求也纳入统计。

最终 11 张中仅 4 张能自动落笔。编码助手查看这四张前后图：机器人胸前纽扣、火箭舷窗初步合理；猫舌头偏在嘴边且未连接；椅子被当作画板，添上的“铅笔”只是无关横线。因此**目前不能声称稳定有效共创、准确率达标或整体体验已经改善**。模型复核放行并不代表语义正确，当前尚无独立人工盲评的语义通过率。

复跑命令（`--live` 会产生供应商用量）：

```bash
node server/scripts/check-nilo-cocreate.mjs --live --suite=smoke --check-canvas --details
node server/scripts/check-nilo-cocreate.mjs --live --suite=holdout --retest --check-canvas --details
node server/scripts/check-nilo-cocreate.mjs --live --local --case=robot --check-canvas
```

不带 `--live` 只生成合成样例。`--legacy` 对照旧协议；所有模型对照应使用相同冻结图片和相同调用/时间预算。输出放在忽略目录 `frontend/.tmp/nilo-cocreate/`，每轮聚合另存时间戳；只将无儿童信息的汇总纳入仓库。

质量评审须分别检查：主体识别、细节相关、位置/连接/比例、是否覆盖或重复、画风一致、孩子能否看懂。独立人工未评审时 semantic 保持未评估；每轮计入失败与超时。本地恢复选择、预览、确认分别记为 `recovery_offered` / `recovery_previewed` / `recovery_accepted`，不计为 `ai_committed`。

最后确认本地后端已重载、健康接口正常；单次本地 HTTP 合成机器人检查返回 v2，5863 ms、一次纠正后胸口圆灯通过实际画布检查。这次只验证部署中的代码链路，未替换上述固定批次结果，也不追加到其成功率中。

在仓库根目录使用 Node.js 24：

```bash
npm --prefix server ci
npm --prefix frontend ci
```

复制 `server/.env.example` 为 `server/.env`，使用目标服务实际支持的视觉模型；语音与文献 embedding 分别配置，不复用绘画密钥。具体变量见[部署](部署.md)和 [Nilo API](../server/NILO_API.md)。

两个终端分别启动：

```bash
npm --prefix server run dev
```

```bash
npm --prefix frontend run dev
```

官网为 `5173`、家长端 `5174`、儿童端 `5175`，三者代理 `/api` 到 `3001`。前端命令不启动后端。开发后端应自动重载，避免前端更新后仍访问旧接口。

- `/api/health` 返回 `{ok:true}` 只说明进程响应，不能证明模型可用。
- `node server/scripts/diagnose-nilo.mjs` 默认只检查配置就绪情况；`--live` 才会用合成图发起一次真实模型请求并产生用量，不应混入普通自动化检查。
- 手机联调将三端地址设为设备可访问的地址，不能让手机跳转到自己的 localhost；跨域 API 需配 CORS。生产麦克风要在 HTTPS 与真实授权下验收。

## 2. 自动化检查

```bash
npm --prefix server test
npm --prefix frontend test
npm --prefix frontend run build
npm --prefix frontend run lint
node knowledge/scripts/validate.mjs
```

修改单个模块时先跑相关用例，再按影响范围执行全量检查。独立端发布还需验证对应 `build:parent` / `build:child`；三种构建默认共享 dist，需要分别指定输出目录或及时部署产物。

| 改动范围 | 重点测试位置 |
| --- | --- |
| 比例、碰撞、整组提案 | `frontend/tests/companion-proposals.test.ts`、`companion-controller.test.tsx` |
| 笔迹、来源、保存与撤销 | `drawing-canvas.test.tsx`、`companion-scene.test.ts`、`nilo-drawing-integration.test.tsx` |
| 尺寸与工具栏 | `drawing-surface.test.ts`、`drawing-surface-resize.test.tsx`、`drawing-tools.test.tsx` |
| 语音与音乐 | `companion-voice.test.tsx`、`drawing-music.test.tsx` |
| 语言、引导、会话、家长视图 | `i18n.test.tsx`、`onboarding.test.tsx`、`authFetch.test.ts`、`parentViews.test.tsx` |
| 共创、语音、限流 | `server/tests/niloDialogue.test.js`、`voice.test.js`、`security.test.js` |
| 数据、报告、周期与恢复 | `server/tests/artworks.test.js`、`period-data.test.js`、`regressions.test.js` 及相关服务用例 |

测试结果应记录提交号、命令、环境、通过/失败项和未覆盖部分。旧记录中的测试数量不是当前门槛，构建通过也不代表语音服务或真实设备已验收。

## 3. 真实家庭主流程

使用新建测试家庭及测试画作，不为演示修改已有家庭或真实创作时间。

1. 从官网分别点击双端入口，确认直接打开对应登录页，并可返回官网。
2. 注册家长账号，复制邀请码；另一浏览器或独立配置文件注册儿童账号并加入。无孩子、无作品和网络失败应分别显示，不能用游客数据填充正式家庭。
3. 家长填写孩子生日；未填生日时不猜测年龄。创建第二个测试孩子，切换时确认作品与异步结果不串号。
4. 孩子从大门新建，画几笔后点“保存”。儿童历史能续画，家长分析历史此时不应新增记录。
5. 点“完成”，确认先保存再分析。成功响应含 `analysisId`，画布留在原页显示反馈；分析失败时已保存作品仍可打开。
6. 家长电脑端点击最近作品右上日期/箭头展开完整解读，选择对应记录，生成并回看报告。“本周成长摘要”的查看详情只是进入创作记录。
7. 在另一设备或重新登录后确认历史原图与报告可读，不依赖儿童浏览器缓存。生成/删除解读后相关概览及时更新。
8. 直接访问家长端 `/parent/archive`，导出 HTML，离线检查图片、日期和已有解读；另用浏览器打印检查 PDF。当前主界面尚缺档案跳转按钮。

## 4. 共创、语音与画布

| 检查 | 预期结果 |
| --- | --- |
| 首次进入空白画布 | 选择自己画/一起画；历史作品直接续画，不强制半圆热身或自动多步工具引导 |
| 画船并说“这是飞船” | 以孩子明确叙事为优先，建议与当前作品相关；语义质量需真实模型单独评测 |
| 点击 Nilo 交接回合 | 不申请麦克风、不播放 TTS；约 1.2 秒逐笔投影后等待确认，确认一次提交整组，撤销一次恢复 |
| 点击识别失败、断网或超时 | 识别不确定、无有效方案、网络失败或 16 秒超时时，多笔画场景保留原画并提示重试，不复制最后一笔；仅简单起笔保留本地几何反馈。迟到响应不能落笔，没有空间不能覆盖原画 |
| 局部图与共创质量 | 最新对象放大而不改变布局；细小笔迹和画布边缘连接坐标正确；先画心形再画苹果时发展新对象；未命名曲线可续画，明确车轮不变成气球；AI 提交与本地兜底分别统计 |
| 本地接笔的取消与来源 | 动画期间继续画、撤销、切作品或退出一起画，旧笔迹不提交；已提交内容可保存，标记为 Nilo，不进入孩子独立笔迹分析 |
| 1–4 元素组合 | 物理比例合理，附近避让；移动、缩放、换色作用于整组，确认一次提交，Nilo 撤销一次恢复 |
| 模板外的自定义物体 | 请求机器人、恐龙等物体，检查自定义轮廓是否可辨、关键细节是否清楚，与儿童笔刷一致；手机与桌面保持物体比例 |
| 自定义协议与预算 | 无效路径、越界坐标、超出 24 路径/物体或 64 笔迹/组时整组拒绝；拒绝一个物体后可继续请求其他自定义物体 |
| 笔刷风格与高分屏 | 沿用孩子笔刷、颜色和粗细；不同 DPR 下预览与确认后的路径和纹理一致 |
| 投影期间保存/下载 | 投影与引导轮廓不进入导出；继续画、切作品、取消或改尺寸使旧结果失效 |
| 保存、重开共创作品 | 保留笔迹文档和分组撤销；家长展示合成图，成长分析只使用孩子层，并标记共创影响 |
| 旧 PNG 缺来源 | 可续画、保存；不伪造作者归属，不直接生成新的儿童特征分析 |
| 麦克风与播报 | 主动开始；显示识别结果，可结束本段、打断播报、停止；关闭播报不禁用收音能力 |
| 确认短语 | “留下来”才提交；“好看”“我喜欢”“不要留下来”“好看吗”不误提交 |
| 本地调整 | “小一点”“往左”“换成蓝色”等不请求绘画模型；换想法可能产生新请求 |
| 连续交流与离页 | 主动开启，达到轮数/时长上限或停止、切后台后释放录音与播放，不自行恢复 |
| 识别失败或权限拒绝 | 提示真实原因，可以重试或使用按钮；不会把无识别结果当作确认 |
| 音乐 | 顶部独立控制，语音期间降低音量，结束后恢复用户设置 |

真机语音验收分别测试中文/英文、儿童发音、噪声、权限、设备麦克风及供应商网络。浏览器有识别 API 或 `/voice/config` 返回 true，不等于实际识别可用；云端与浏览器降级分别验收。

## 5. 设备、语言与无障碍

- 在桌面、横竖平板、小手机上检查画布、底部按钮、调色盘和英文长文案。展开/收起工具时记录画布坐标与尺寸，二者应一致；真实窗口变化时保持原画比例。
- 手机儿童端尝试横屏但不阻断；不支持方向锁定时页面仍可用。实测 Safari、目标安卓浏览器及安装模式，不将模拟视口当作系统转屏验收。
- 家长桌面保留侧边布局，手机保留独立顶部/底部导航。检查内部滚动和安全区，不能强行把长报告压进一屏。
- 现有手机家长端完整解读入口缺失、概览月报卡的状态含义和静态通知等限制以[功能与使用流程](功能与使用流程.md)为准，不在演示中掩盖。
- 中英切换不清空表单、笔迹或撤销历史；用户昵称和历史自由文本不自动改写。新报告按请求语言生成，存量报告保留原文。
- 新手引导检查可见目标、异步准备、目标消失、实际笔画完成、Escape/Tab/焦点恢复、账号隔离和跳过记录。角色热点检查连续点击取消、键盘操作及减少动态效果。

## 6. 权限、故障和数据生命周期

| 场景 | 预期结果 |
| --- | --- |
| 儿童调用报告、趋势、周期摘要 | 403；自己的分析历史不返回家长报告 |
| 跨家庭读取或删除 | 拒绝访问，不为对方生成报告/摘要 |
| 过期 access 或退出时迟到响应 | 刷新轮换或提示重新登录，不降为游客；旧响应不能恢复会话或写入新账号 |
| 同提交键重试、同键换图、画册版本变化 | 相同内容复用记录；冲突返回错误，不重复记录或覆盖新版本 |
| 特征格式错误、模型断开、增强超时 | 错误可恢复，API 仍健康；报告增强失败保留基础报告，共创不自动付费重试 |
| 共创取消和绘画/语音限流 | 迟到内容不落笔；语音使用独立额度，仍受全局限制 |
| 家长删除分析 | 删除分析副本并使周期摘要失效，儿童可编辑原稿仍在 |
| 家庭注销失败/成功 | 错密码不删、不退出；成功清除家庭的画册、分析、摘要和会话；媒体残留有运维提示 |

周期使用北京时间已结束自然周/月。测试周一边界、闰月、跨年、无数据、来源改变与重复补齐；优先运行隔离的 `period-data.test.js`。`npm --prefix server run reports` 会对实际数据库补齐摘要，不应为验收篡改真实记录日期。

删除与注销使用独立测试家庭；备份恢复使用[部署指南](部署.md)中的新目录，核验原图哈希与删除后恢复风险。语音原文/录音不应写入项目持久日志，导出文件不得包含令牌。

## 7. 记录验收结论

每次发布记录“在哪个提交、哪种设备/浏览器、哪种配置下，通过了哪些场景”。没有实际执行的供应商调用、儿童语义质量、弱网触控、打印和生产恢复应标为未验收；不要将工程测试数量换算为模型准确率或产品效果。

2026-09-20 共创优化验证（本地未提交工作区）：前端 360 项、后端 287 项自动测试通过，生产构建通过；lint 无错误，保留 3 条既有警告。隐藏 Edge 在 1440×900 和 844×390、DPR 2 下验证了实际局部 PNG 尺寸、失败后本地接笔、原画撤销恢复、独立统计和不自动开启语音；这部分 API 使用模拟失败响应。

另用当前配置的 `deepseek-v4-flash-vision-exp` 实际调用合成案例：`heart_then_apple` 在新对象的梗上接叶（1971 ms）、`open_curve` 延伸未命名曲线（2636 ms）、`wheel` 按孩子明确含义添加轴心（2622 ms）。每例最终测试各 2 次模型调用，均通过生产前端路径和连接检查，并输出合成预览。这是小样本功能验证，不是识别准确率、儿童满意度或供应商横向评测；尚未开展真实儿童多轮体验测试。可复现命令及聚合诊断入口见 [Nilo API](../server/NILO_API.md)。

2026-09-20 后续部件编译验证：后端 308 项测试通过，前端共创方案与画布集成 54 项测试通过。新增 9 种常见部件的服务端编译器，合成案例扩展为 11 类。房子补窗户、人物补双眼通过路径校验并人工检查预览；重启本地 3001 后，心形接气球绳实际接口耗时 2088 ms、路径可落笔。小鱼补眼睛虽通过接口与几何检查，但人工预览发现位置靠近尾部；补充方位提示后复测仍未纠正，不能计为语义质量通过。该问题说明现有模型的视觉定位与自复核仍不可靠，不能把接口 ready 或 canvasFits 换算成有效共创比例。

本次本地后端原为 `node src/index.js` 的旧进程，已改以 `node --watch src/index.js` 启动并检查健康接口。导入的代码修改触发重启；运行时通过文件读取并缓存的 skill/案例变更仍应显式重启后端。

2026-09-20 添画前后复核：后端在单线程测试池中 318 项通过；Windows fork 测试池曾出现 IPC 通道中断，改用 `npm test -- --pool=threads --maxWorkers=1 --no-file-parallelism` 完成验证。前端全量 362 项中初次新增用例发现助手落笔记录挤出孩子叙事，修复后控制器 57 项重跑通过，其余 305 项此前通过。

14 类合成案例最终一轮有 11 类通过生产画布几何检查；这不是有效共创率。人工检查左右朝向小鱼的眼睛均在头部、房屋窗户位于墙面、人物双眼位于头部；已有眼睛的小鱼补了背鳍而未重复补眼。竖向鱼仍有歧义，新旧对象之间的叶片连接与开放曲线仍出现拒绝或几何失败。保留这些失败，不通过放松覆盖原画规则提高通过数。此次调用每例 2–4 次模型请求，约 2.1–5.5 秒，仅为本机当前模型配置的单轮合成结果。

2026-09-20 人物添画资源验证：人工筛选并接入 5 张 Quick, Draw! 参考与 1 张原创刘海示例，部件编译器由 9 种扩为 15 种，文字案例由 6 类扩为 7 类。多笔画改用整幅主体规划；连通的头部不再因为最后一笔位于腿部而被注意范围检查拒绝。无效部件方案可使用原有的一次纠正机会；内部细节在视觉复核前允许最多画布短边 3% 的小范围像素避让，不改变大小、不移动已复核结果、不放松客户端碰撞保护。

新增 `large_person` 合成案例采用 2:1 横向画布、深绿色笔迹和闭眼微笑大头，重建布局而非使用原始儿童图片。最终直连两次测试：一次刘海方案可落笔（3105 ms），另一次经格式修正后被位置复核拒绝（4139 ms）；保留失败，不能据此声称稳定有效。已修复的偏左刘海旧方案可用真实前端几何检查离线回放通过。后端重载后，localhost:3001 实际接口一次返回刘海并通过画布检查（5325 ms），人工查看了预览。后端完整 333 项测试、前端共创方案和画布集成 54 项测试通过。参考是推理时的视觉输入，不是训练；来源和许可见[绘画资源说明](../server/skills/nilo-cocreate/references/drawing-resources.md)。

2026-09-20 绘画知识库接入：28 个主题条目，人工查看 87 张 Quick, Draw! 候选后保留 29 类 57 张参考，配套 13 组原创笔画步骤。儿童端点击开启视觉观察→主题检索→规划→复核流程；一次观察上限 650 tokens，正常三次、最多五次模型调用，仍共用原 14 秒预算。多笔画或无可追溯笔迹的旧原画不再以本地复制线条兜底。

初次合成测试暴露供应商把 visible 返回为布尔值、导致条目未检索的问题，已改为 evidence 文本并兼容有有效边界和部件列表的旧格式，增加回归测试。修复后已确认猫参考图实际进入规划；添加原创胡须步骤后模型提出相关胡须，但连接点仍偏离脸颊，前端拦截（3182 ms），不能计为语义或落笔成功。人物补鼻子通过几何检查（3443 ms），人工预览显示位置仍偏右，不能声称有效共创已解决。最终本地 HTTP 房屋测试确认观察调用和 hasReference=true 日志，窗户位于墙面并通过画布检查（3029 ms）。

最终后端 341 项测试、前端共创控制/几何/画布集成 114 项测试通过，前端 TypeScript 与生产构建通过。此验收证明知识检索链路、资源来源和失败保护工作，不是儿童画准确率或任意共创能力保证。资源更新已通过实际后端重载生效；本地未推送。

2026-09-20 点击后不落笔排查：近期七次请求均检索到知识，仅两次进入服务端 preview，其他请求被识图、格式、结构或位置检查中止；preview 不等于前端落笔成功。合成机器人案例复现“已识别且有纽扣方案，但 grounding.visible 返回列表导致误判未看清”。现已支持无损合并文本列表；高置信度但描述类型错误使用现有一次格式纠正。无关的可选 structure 不再否决普通部件，眼睛、嘴、窗户的必要结构检查保持不变。

同时开放观察主体名称，增加八类结构指导，图库外物体不强制归为抽象；自定义部件支持相对位置/比例，并可选择从实际原图提取的连接点。未新增第三方图片，未训练模型，也未增加调用上限。完整后端 353 项、前端方案与画布集成 54 项测试通过。

最终一轮合成机器人直连测试三次调用、4233 ms，在身体内添加纽扣并通过生产画布几何检查；猫脸五次调用、5839 ms，经位置纠正可落笔，但人工查看仍只有一根偏高的胡须，不算自然完整的添画。另一个吉他样例因识别置信度不足未落笔，保留失败。后端重载健康检查通过；首次本地 HTTP 验证遇到 ECONNRESET，健康确认后重试机器人接口成功（3758 ms），纽扣位于身体内。以上只证明部分无落笔原因得到修复，不代表任意绘画稳定共创或真实儿童多轮验收通过。本地未推送。

2026-09-20 整幅画接力与失败后互动：近期三次用户请求分别出现 invalid_contract、missing_proposal 和 wrong_target，日志不保留原始画面/模型文字，不能将三次失败归为同一原因。新增合成 tree_scene（树、云及两条分离的天空线），初次复现模型以 string 部件表达树皮、纠正后缺少 target。点击新增 scene 范围，允许接现有儿童主体而非强制最后几笔；缺失目标标签可复用已有识图证据，部件 string 对模型改名 balloon_string，旧别名只保留兼容。树的参考增加原创树节路径，原创步骤总数现为 14 组。

树的实际模型复测仍未稳定落笔：第二次 3522 ms 返回叶片并通过模型复核，但被生产画布几何检查拒绝；第三次 5662 ms 因关联复核失败停止。这些失败保留，不计为有效共创。前端针对失败、离线和超时提供两个本地可点选画法，先显示投影，经孩子确认后落笔；选择不调用模型、不启用麦克风，继承笔刷并保留碰撞、版本、取消和整组撤销保护，画布已满则不强行覆盖。

验证：后端全量 355 项通过；部件明确命名后相关 96 项重跑通过（包括一个新增命名兼容用例）。前端控制器、恢复画法与真实页面组件/画布集成共 97 项通过，TypeScript 和生产构建通过。包含完整点击选择、确认及一次撤销测试；桌面/手机几何尺寸通过自动测试，当前电脑控制工具未提供浏览器，因此未进行此轮真实浏览器截图验收。基础画法可用不等于模型共创质量已经达标。


### 整幅画面共创与对象编辑（2026-09-21）

本轮将开放交接优先规划为完整相关事物，也保留原有细节方式。新增 6 类 18 种原创可执行画法，每类 3 种结构变体；原有知识条目、参考拼图仍参与。完整物体按场景关联放置，不要求附着原画；名称与 recipeId 主体不一致会被拒绝，防止把海草错误映射为小船。

点击和语音新画统一投影后确认。Nilo 已提交分组可选择、换色、缩放、移动、换内置画法或预览删除；内置部件有标注时支持局部调整。同名对象显示编号供选择，不能偷偷修改最后一个。修改以独立 edit 事件保存，保持原层次与儿童笔迹；取消、撤销和保存重开均已测试。旧 PNG 无独立对象信息，不承诺恢复对象编辑。

验证记录：前端全量 400/400 通过；最后补充确认短语及初始编辑预览处理后，相关 137/137 重跑通过。后端全量最初 580/586 通过，6 个失败为旧 token 上限和禁止引用已有 Nilo 对象的预期；更新预期后相关 102/102 通过，其余测试在全量中通过。TypeScript/生产构建通过；lint 0 错误、3 条既有警告。离线渲染检查全部 18 种画法。真实页面组件测试覆盖浏览器语音识别事件→编辑已提交松鼠→取消/确认→保存/重新打开→撤销；语音识别硬件与真实儿童发音未在本轮重新验收。

现有供应商对同两张合成画进行了三轮开发复测：第一轮 1/2 可进入画布，第二轮 0/2，修复完整物体最小尺寸与空位搜索后第三轮 2/2；所有失败保留于 server/evals/nilo/2026-09-21-scene-upgrade.json。最后一轮人物旁画出完整小鸟，鱼增加侧鳍；助理查看了前后图。之后追加的主体/recipeId 对齐防护通过离线回归。这些是在同样本上迭代的结果，不是独立准确率，也没有证明任意儿童画稳定共创。没有接入新供应商，编号定位实验仍未默认开启。

本地后端以 node --watch src/index.js 启动，localhost:3001/api/health 返回 ok:true。官网、家长端和儿童端开发服务已启动（5173/5174/5175）。当前修改未提交、未推送。发布时前后端须一起更新，旧客户端无法解析新增 edit 事件。


### 2026-09-21 开放共创 v3

当前儿童端启用 v3：整幅画观察与知识检索后，模型自主选择完整相关物体，可用知识库画法或自定义笔画。不再以语义复核、主体置信度、现实关系或少量叠线否决方案；旧 v2 严格管线保留用于兼容和历史实验。模型按构图选尺寸，程序保比例并控制边界／可读性。移除叶子／云朵菜单和本地随意短线，技术故障如实显示。

新增测试覆盖不确定主体、目录外物体、模型指定备用画法、低信息短线、命令字符串解析、自由叠线、空白优先、场景尺度、手机比例、格式修复及网络失败。投影仍需确认，画布版本与取消检查不放宽。

验证：后端完整 598/598，通过前端受影响四套测试 137/137，生产构建通过；ESLint 0 error、3 条已有 warning。现有接口对同两幅合成图进行了三轮开发回归，共 6 次请求：第一轮风筝成功／鱼格式失败；第二轮可出图但长椅位置和泡泡形状欠佳；最后一轮人物旁权杖、鱼嘴旁三个圆泡泡，分别 3488/2820 ms，各 2 次调用。最后一轮权杖没有接到手、泡泡向右下排列；不宣称任意画面语义正确。完整结果摘要在 `server/evals/nilo/2026-09-21-creative-turn.json`，各轮图片和原始计划在被忽略的 `server/.tmp/creative-live*` 下。未推送。


### 自主配色、54 种素材与投影拖动

扩展至 18 类 54 种原创完整线稿，全部已渲染并查看；模型自行选色，不再被当前儿童画笔色覆盖，保留笔触和粗细。已生成 `docs/assets/nilo-recipes.svg`，可通过 `server/scripts/render-nilo-recipes.mjs` 重建。投影可触摸／鼠标拖动，右下角手柄等比缩放；拖动取消恢复原位，整组确认后可保存、重开和撤销。

验证：后端完整 636/636；前端相关五套 122/122；TypeScript 与生产构建通过；ESLint 0 error、3 条已有 warning。现有接口两次合成画作回归返回金色皇冠（#d9a827，3568ms）和蓝色泡泡（#328ab5，2926ms），均为 2 次调用，证明颜色能端到端保留，不能据此推导所有画面的共创质量。未进行真实手机硬件触摸测试；触摸坐标、捕获、取消和保存由交互测试覆盖。未推送。

### 270 种原创画法扩充（2026-09-21）

现有 54 种扩充至 90 类 270 种，新增动物、自然、交通建筑、食物、物件玩具和幻想角色。所有画法 ID 与姿态名都可供模型选择，只有前 12 个相关物体展开关联和配色；请求不包含整套路径数据，不增加模型调用轮次。目录外自定义绘图保留。语音按共享中英文名称选择对象，支持全部 90 类的换画法，避免鲸鱼／鱼、熊猫／猫等名称重叠误选。

270 种路径均通过几何检查，全部生成并人工查看联系图，修正头身连接、手鼓轮廓等问题。后端全量 860 项、前端共创相关 5 套 213 项测试通过；类型检查和生产构建通过，lint 0 错误、3 条既有警告。测试只证明可执行性和交互逻辑，不证明任意场景的语义准确率。

现有真实接口在两张合成画作上接收明确请求：人物旁恐龙选中 dinosaur-0（绿色，2532 ms），小鱼旁章鱼选中 octopus-1（粉紫色，2419 ms），均两次模型调用并生成投影。已查看实际合成预览；恐龙与人物手臂有少量叠线，可在确认前拖动。这是两例冒烟检查，不是儿童体验或稳定成功率评测。记录见 [扩充验收](../server/evals/nilo/2026-09-21-recipe-expansion.json)，[完整素材预览](assets/nilo-recipes.svg)。
