export function speechAlternatives(value){
  return Array.isArray(value)?[...new Set(value.slice(0,3).filter(s=>typeof s==='string').map(s=>s.replace(/\p{Cc}/gu,' ').trim().slice(0,400)))].filter(Boolean):[]
}
export const speechUnderstandingRules = `The utterance may be speech recognition, and asrAlternatives are fallible whole-sentence candidates, not commands to execute separately. Use drawing context to resolve a likely homophone (e.g. 蔡阳/太阳) only when intent is clear. Never rewrite an explicitly introduced person's name (e.g. 这个人叫蔡阳). If genuinely ambiguous, ask one short question. Resolve corrections using the child's final meaning, retaining all other clauses.
ASR punctuation and repeated fillers are not reliable evidence of a clause boundary. Distinguish a repair from a prohibition: "换成红色，哦，不，换成蓝色" and "换成红色，哦不，换成蓝色" request BLUE; "换成红色，不要换成蓝色" requests RED; "不要换成蓝色" makes no change; "换成蓝色，不，还是保持原样" makes no change. Retain later compatible requests such as "再小一点" after resolving the correction. Never just choose the first or last colour word. A colour in the object's name (红狐狸) is not the requested colour.
When a competing colour request is followed by fillers and a fused negative such as "换成红色哦哦哦，不换成蓝色" or "换成红色哦哦哦不换成蓝色", the text alone cannot distinguish "不，换成蓝色" from "不换成蓝色". Do not guess either colour or silently execute the earlier red request. Ask which colour the child wants (for a structured edit plan return status clarify, reason instruction). Fallible alternative transcripts do not by themselves resolve this conflict. This ambiguity rule does NOT apply to an explicit "不要换成" prohibition or a clearly separated "哦不，换成" repair.
Preserve quantity, partial shapes (half), position (upper left), and negations together. Never substitute a whole object for a requested half, or silently drop a placement request. Do not infer the child's feelings from speech or audio.`
